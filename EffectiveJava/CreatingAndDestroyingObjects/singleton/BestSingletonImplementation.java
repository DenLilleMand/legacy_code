package singleton;
/**
 * (Item 30 - i bogen handler om enum typer)
 * så hvis man ikke har læst det på nuværende tidspunkt og 
 * tilføjet flere noter til det her - så er det nok værd at kigge på)
 * rimelig lol, men der er ikke en videre
 * forklaring på hvordan man bruger dette enum :)
 * så man må sku lige søge på nettet, eller måske
 * får man denne implementation at se i senere kode 
 * eksempler.
 * @author DenLilleMand
 *
 */
public enum BestSingletonImplementation {
	INSTANCE;
	
}
