public class NoterBinaryTrees {
	/**
	 * complete binary tree = hvis et binært træ er helt fyldt med 2 non-null
	 * børn ved hver node.
	 * 
	 * decision tree: which is a binary tree in which all nodes have either zero
	 * or two nonempty children
	 * 
	 * 
	 * binary search trees, also called ordered binary trees,
	 * 
	 * 
	 * A binary search tree has the following property: For each node n of the
	 * tree, all values stored in its left subtree (the tree whose root is the
	 * left child) are less than value v stored in n, and all values stored in
	 * the right subtree are greater than v.
	 */
}
