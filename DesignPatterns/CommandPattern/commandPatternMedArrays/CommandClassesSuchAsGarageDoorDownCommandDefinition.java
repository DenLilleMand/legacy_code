package commandPatternMedArrays;
/**
 * De her command klasser extender vores
 * Command interface, 
 * og deres definition på side 219 lyder:"Using the 
 * Command Interface, each action that can be invoked by pressing a button
 * on the remote is implemented with a simple Command
 * object. The Command Object holds a reference to an
 * object that is an instance of a Vendor Class and implements an execute method that calls one or more methods on that object.
 * Here we show two such classes that turn a light on and off, respectively".
 * @author DenLilleMand
 *
 */
public class CommandClassesSuchAsGarageDoorDownCommandDefinition {

}
