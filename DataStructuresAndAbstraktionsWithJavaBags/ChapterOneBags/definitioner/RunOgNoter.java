package definitioner;

public class RunOgNoter {
	/**
	 * Definitioner på de almene koncepter i den her bog:
	 * 
	 * A data type such as int or double is a group of values and
	 * operations on those values that is defined within a specific programming language.
	 * 
	 * In contrast, an abstract data type(or ADT) is a specification for a group of values and the 
	 * operations on those values that is defined conceptually and independently of any programming language.
	 * A data structure is an implementation of an ADT 
	 * within a programming language.
	 * 
	 * så ADT er abstract imens en data structure er en konkret implementation,
	 * sagt med begreber vi kan forstå.
	 * 
	 * Collection: is an object that groups other objects and provides various services to its
	 * client. In particular, a typical collection enables a client to add, remove, retrieve and query 
	 * the objects it represents. Various collections exist for different purposes. Their behaviors are
	 * specified abstractly and can differ in purpose according to the collection.
	 * Thus a collection is an abstraction and is an abstract data type. 
	 * However, an ADT is not necessarily a collection.
	 * 
	 * Det virker til at de prøver at lede os hen på at det vi for det meste arbejder med i den her bog er deres
	 * Bag som er en collection hvilket gør det til en ADT.
	 */
}
