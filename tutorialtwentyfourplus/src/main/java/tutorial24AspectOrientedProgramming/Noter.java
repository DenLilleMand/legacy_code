package tutorial24AspectOrientedProgramming;

public class Noter 
{
	/**
	 * Noter:
	 * 1.write Aspects.
	 * 2.Configure where the aspects apply.
	 * 
	 * 
	 * man kan bruge Aspected Oriented Programming til funktioner så som logging,
	 * eller transactions(ikke mysql), det er designet til sådanne ting hvor det ikke
	 * kun gælder for en klasse, men for flere klasser, f.eks hvis man vil kører logging i hele
	 * ens system, så er der jo alt alt for mange dependencies til logging rundt omkring i systemet på kryds og tværs.
	 * det er det vi prøver at undgå her ved at holde alt vores logging kode 1 sted, så når noget ændre
	 * sig, så skal vi ikke ud i alle klasserne og ændre kode.
	 * 
	 * 
	 * 
	 */
}
