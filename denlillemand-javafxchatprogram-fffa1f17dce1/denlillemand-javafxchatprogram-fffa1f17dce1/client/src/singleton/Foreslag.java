package singleton;

public class Foreslag {
	/**
	 * public enum Elvis {
INSTANCE;
private String[] favoriteSongs =
{ "Hound Dog", "Heartbreak Hotel" };
public void printFavorites() {
System.out.println(Arrays.toString(favoriteSongs));
}
}

Effective javas Singleton kapitel, der er nok ikke andre som har taget en enum singleton med.

Så skal vi selvfølgelig også håndtere lazy/eager initialization.
	 */
}
