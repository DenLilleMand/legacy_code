package servlets;

public class Noter {
	/**
	 * foreslag til dette projekt:
	 * Lav det til en chatClient, der bare viser chatten der foregår i programmet offline.
	 * Ved endnu ikke om der er nok tid til også at kunne give servlet en chance for at
	 * skrive input.
	 * 
	 * 
	 * 
	 * 
	 */
}
